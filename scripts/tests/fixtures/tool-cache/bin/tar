#!/usr/bin/env bash

set -euo pipefail

destination=""
while [[ $# -gt 0 ]]; do
    if [[ "$1" == "-C" ]]; then
        destination="$2"
        shift 2
    else
        shift
    fi
done

mkdir -p "$destination"
printf '#!/usr/bin/env bash\necho 1.7.12\n' > "$destination/actionlint"
