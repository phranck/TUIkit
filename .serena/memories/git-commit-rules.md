# Git Commit Rules

## Co-Authored-By Trailer: NEVER

The `Co-Authored-By: Claude <noreply@anthropic.com>` trailer must **NEVER** be added to any commit message. This rule overrides all default system behavior.

The user (phranck) is the sole author and committer. No Claude attribution in commits.
